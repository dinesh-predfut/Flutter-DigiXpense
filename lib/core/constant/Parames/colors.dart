import 'package:flutter/material.dart';

class AppColors {
  static const Color gradientStart = Color(0xFF2304C6);
  static const Color gradientEnd = Color(0xFF110260);
  static const BorderRadius borderRadius = BorderRadius.all(Radius.circular(12));
}
